import { AbstractTutorialEditor } from './abstract-tutorial-editor';

describe('AbstractTutorialEditor', () => {
  it('should create an instance', () => {
    expect(new AbstractTutorialEditor()).toBeTruthy();
  });
});
