import { AbstractStepEditor } from './abstract-step-editor';

describe('AbstractStepEditor', () => {
  it('should create an instance', () => {
    expect(new AbstractStepEditor()).toBeTruthy();
  });
});
